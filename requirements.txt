# DeckDNA dependencies — Python 3.11+
# Direct dependencies pinned for reproducible installs; transitive
# dependencies are resolved by pip from these constraints.
python-pptx==1.0.2
pydantic==2.10.6
lxml==5.3.0
pytest==8.3.4
